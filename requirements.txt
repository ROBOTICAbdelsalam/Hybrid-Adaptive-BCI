# Hybrid-Adaptive-BCI — Python dependencies
#
# Target interpreter: Python 3.12
# Verified working in the project virtual environment (.venv).
#
# Setup:
#   python3.12 -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt
#
# Versions are pinned to the set the pipeline was validated against.
# ROS2 build tooling (colcon, rosdep, ...) is NOT listed here; it is
# provided by the ROS2 distribution. See requirements-ros2.txt.

# --- EEG loading and preprocessing ---
mne==1.12.1
pooch==1.9.0          # required by mne.datasets for EEGBCI download

# --- Numerical / data ---
numpy==2.5.1
scipy==1.18.0
pandas==3.0.3

# --- Visualization ---
matplotlib==3.11.0

# --- Machine learning (CSP, SVM, RF, splitting, metrics) ---
scikit-learn==1.9.0
joblib==1.5.3
xgboost==3.3.0

# --- Deep learning (CNN / LSTM / CNN-LSTM) ---
tensorflow==2.21.0

# --- Testing ---
pytest==8.3.4
pytest-cov==6.0.0
